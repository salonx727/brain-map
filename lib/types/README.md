# Shared types (Phase 2 — implemented)

`canonicalNode.ts` holds `CanonicalNode` (`ScreenNode` | `EngineNode`), `FieldValue`
(present/empty/disconnected/unknown), `AttributedItem`, and `Diagnostic`. PM record types
(todo/note/decision/owner/figma-link/file) land here once the Supabase schema exists
(Phase 4).
